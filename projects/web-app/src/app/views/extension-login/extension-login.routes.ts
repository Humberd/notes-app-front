import { Routes } from '@angular/router';
import { ExtensionLoginComponent } from '@web-app/app/views/extension-login/extension-login.component';

export const routes: Routes = [
  {
    path: '',
    component: ExtensionLoginComponent,
  },
];

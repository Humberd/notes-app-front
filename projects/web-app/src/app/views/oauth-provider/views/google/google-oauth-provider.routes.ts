import { Routes } from '@angular/router';
import { GoogleOauthProviderComponent } from '@web-app/app/views/oauth-provider/views/google/google-oauth-provider.component';

export const routes: Routes = [
  {
    path: '',
    component: GoogleOauthProviderComponent
  }
]
